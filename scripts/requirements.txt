# Pinned to the versions the production parquet + dashboard were generated
# against. Daily CI runs on Python 3.12 (.github/workflows/daily-check.yml);
# local development runs on Python 3.14. If a new package release breaks the
# pipeline, bump the pin deliberately rather than letting CI publish a silent
# regression. `scripts/test_backtest.py` is the regression backstop.
ccxt==4.5.56
pandas==3.0.3
numpy==2.4.6
pyarrow==24.0.0
matplotlib==3.10.9
requests==2.34.2

# --- test suite (tests/, pytest) — dev/CI dependency, not used by the pipeline
# or the parquet build. Wired into CI in the Phase-6 cosmetic-A uplift.
pytest==9.0.3
